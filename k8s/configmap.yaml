apiVersion: v1
kind: ConfigMap
metadata:
  name: mlflow-configmap
data:
  # property-like keys; each key maps to a simple value
  DEFAULT_ARTIFACT_ROOT: s3://mlflow/
  DB_NAME: postgres
  DB_USERNAME: postgres
  DB_HOST: data-sci2.movio.co